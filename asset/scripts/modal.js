'use strict'

function mdOpen(mdName) {
    document.querySelector(mdName).showModal();
}

function mdClose(mdName) {
    document.querySelector(mdName).close();
}

function toastOpen(toastName) {
    let toast = document.querySelector(toastName);
    
}